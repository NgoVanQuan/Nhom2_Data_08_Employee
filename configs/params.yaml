data:
  raw_path: "data/raw/HR_Analytics.csv"
  processed_path: "data/processed/hr_cleaned.csv"

# Tham số chia tập dữ liệu
split:
  test_size: 0.2
  random_state: 42

# Tham số cho thuật toán Khai phá (Apriori/FP-Growth)
mining:
  min_support: 0.05
  min_threshold: 1.0 # Dành cho chỉ số lift